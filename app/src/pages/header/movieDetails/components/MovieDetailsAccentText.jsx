import styled from 'styled-components';
import { VARIABLES } from '@styles/VARIABLES';

const MovieDetailsAccentText = styled.p`
    margin: 20px 30px 20px 0;
    color: ${VARIABLES.red};
    font-size: 25px;
`

export { MovieDetailsAccentText }