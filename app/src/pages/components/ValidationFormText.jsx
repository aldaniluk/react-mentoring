import styled from 'styled-components';
import { VARIABLES } from '@styles/VARIABLES'

const ValidationFormText = styled.div`
    margin-top: 5px;
    color: ${VARIABLES.red};
`

export { ValidationFormText }